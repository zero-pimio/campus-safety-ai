# GitHub 发布前核验（2026-09-23）

发布目标为现有公开仓库 `zero-pimio/campus-safety-ai` 的 `main` 分支，核对时远端基线为 `a744d9e5bbd9e1593153579bda228d937768d463`。本次提交新增持续跌倒分析、RTSP 重连、Outbox 容量与磁盘保护、拒绝事件恢复、训练与评估入口、门禁设计及演示复现文档。

## 本地检查

- 安装完整可选依赖的环境：499 项测试、320 个子测试通过，无跳过；10 条警告为 Torch tracing / ONNX exporter 弃用提示。包含本地真实 benchmark 文件一致性检查。
- 全新最小环境，仅安装项目及 pytest：338 项测试、233 个子测试通过，56 项因缺少视觉、训练依赖或真实 benchmark 输入而跳过。
- 无 NumPy 环境中的跌倒源、入口与存储保护模拟测试：30 项通过，无跳过。
- Ruff、`pip check`、`git diff --check` 通过。发布时未替换默认训练模型，未改变停车规则阈值。

最小安装检查暴露出姿态运行模块顶层导入 NumPy 导致测试收集失败的问题；已改为在真实模型构造或预测时导入。依赖训练组件的历史 URFD 测试仅在相关顶层包确实缺失时跳过，其他导入异常继续报错。GitHub Actions 安装 pytest 后执行同一测试入口，并在 Python 3.11 / 3.12 上运行最小依赖检查；可选模型回归由本地完整环境结果提供。

## 发布文件边界

仅提交代码、配置、模板、数据清单、测试及文档。原始素材、实验权重、姿态缓存、Outbox、运行目录、新生成的 MP4 与截图保持在本地。新素材没有明确的公开再分发授权时，提供作者来源和本地生成步骤；历史已发布且注明许可的 URFD 展示资产保持原样。

新文档的本机个人绝对路径已转换为仓库相对路径；本地生成产物以路径文本标记，不充当 GitHub 下载链接。原始本地文档备份与本轮测试日志保存在被忽略的 `runtime/publication-20260923/` 和 `runtime/checks/`。模型、数据与原始运行文件的哈希内容未因文档整理而改变。待发布文件的凭据形态检查没有发现实际密钥；测试中的虚构 RTSP 凭据用于验证日志脱敏。

最新演示入口见[演示与本地复现](../../demos/README.md)。两类闯卡仍仅完成设计，摄像头长期稳定性、真实平台联调和 RK3588 全流程实测尚未完成；本次 GitHub 发布不表示部署或场景精度验收通过。
